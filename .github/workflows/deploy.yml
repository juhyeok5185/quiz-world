name: Build and Push Docker Image

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: DockerHub 로그인
        run: |
          echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and Push to DockerHub
        run: |
          docker buildx build --platform linux/amd64 \
            -t ${{ secrets.DOCKER_USERNAME }}/quiz-world:latest \
            --push .

      - name: 🔔 Jenkins 배포 트리거 (Crumb 포함)
        run: |
          CRUMB=$(curl -s -u "danny:118eca0c3987de8250cb411b6ecf0ad573" \
            http://203.245.30.75:9090/crumbIssuer/api/xml?xpath=concat\(//crumbRequestField,%22:%22,//crumb\))

          curl -X POST "http://203.245.30.75:9090/job/thinkdock-deploy/build" \
            -u "danny:118eca0c3987de8250cb411b6ecf0ad573" \
            -H "$CRUMB"
